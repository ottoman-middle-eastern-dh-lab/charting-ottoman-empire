header:
 - titles:
      # @start locale config
      en      : &EN       Project History 
      # @end locale config
    url: /projecthistory.html
  - titles:
      # @start locale config
      en      : &EN       Team 
      # @end locale config
    url: /team.html
  - titles:
      # @start locale config
      en      : &EN       References 
      # @end locale config
    url: /references.html
  - titles:
      # @start locale config
      en      : &EN       Sources 
      # @end locale config
    url: /sources.html
  - titles:
      # @start locale config
      en      : &EN       Methods 
      # @end locale config
    url: /methods.html
  - titles:
      # @start locale config
      en      : &EN       Publications
      # @end locale config
    url: /publications.html
  - titles:
      # @start locale config
      en      : &EN       News
      # @end locale config
    url: /news.html
